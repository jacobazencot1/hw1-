#pragma once

#include <string>

using namespace std;
/*
 template <class T>
class AvlTree<T> {
private:
    struct Node{
        Node* father;
        Node* leftSon;
        Node* rightSon;
        T& data;
    };

public:
    AvlTree();
    void insert (Node* node);
    void remove (Node* node);
    Node* root;

};

 */
